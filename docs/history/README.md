# Documentation History

This directory points to preserved historical records that were replaced by a
shorter current authority. Historical material remains evidence of what was
implemented, tested, or decided at a particular time. It does not override
`docs/APERTURE_CURRENT_AUTHORITY.md`, `docs/PROJECT_STATE.md`, or an explicitly
authorized current task.

The cumulative project state through Current Bootstrap V1 remains immutable in
Git at commit `401e1b0afd6ad2a4678af4f1040014b5c9b568e9`:

- `docs/PROJECT_STATE.md` at that commit
- `docs/CODEX_NEXT_TASK.md` at that commit

Use Git history when auditing the full milestone trail. Do not copy historical
instructions back into the current reading path.
